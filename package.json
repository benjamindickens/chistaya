{
  "private": true,
  "scripts": {
    "dev": "parcel src/index.html --dist-dir public --no-cache",
    "build": "parcel build src/index.html --dist-dir public --no-cache"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.3.1",
    "parcel": "^2.3.1"
  },
  "dependencies": {
    "swiper": "^8.0.6"
  }
}
