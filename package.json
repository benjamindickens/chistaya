{
  "private": true,
  "scripts": {
    "dev": "parcel src/*.html --dist-dir public --no-cache",
    "build": "parcel build src/*.html --dist-dir public --no-cache"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.3.1",
    "parcel": "^2.3.1"
  },
  "dependencies": {
    "accordion-js": "^3.1.1",
    "swiper": "^8.0.6"
  }
}
